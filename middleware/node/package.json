{
  "name": "@stellarflow/x402-middleware",
  "version": "0.1.0",
  "description": "Stellar-native middleware for any HTTP API. Drop-in x402 USDC paywalls for agents + SEP-10 wallet login for operators. One import, one line.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "x402-init": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "x402",
    "stellar",
    "usdc",
    "sep-10",
    "micropayments",
    "middleware",
    "express",
    "agent",
    "mcp"
  ],
  "license": "MIT",
  "dependencies": {
    "@stellar/stellar-sdk": "^13.0.0",
    "jsonwebtoken": "^9.0.2",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.25",
    "@types/jsonwebtoken": "^9.0.5",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "peerDependencies": {
    "express": "^5.2.1"
  },
  "peerDependenciesMeta": {
    "express": {
      "optional": true
    }
  }
}
