{
  "name": "@stellarflow/x402-mcp-stellar",
  "version": "0.1.0",
  "description": "MCP server that lets any MCP-compatible AI client (Claude Code, Codex, Cursor, ...) consume any x402-priced API on Stellar by paying per call in USDC. Includes the agent wallet provisioning + SEP-10 handoff flow.",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "x402-mcp": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "init": "node dist/cli.js init",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@stellar/stellar-sdk": "^15.0.1",
    "@x402/core": "^2.9.0",
    "@x402/stellar": "^2.9.0",
    "bip39": "^3.1.0",
    "ed25519-hd-key": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
